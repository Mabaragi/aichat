# June 2026

## 2026-06-11

- 완료: 문서의 `한 줄 입력 → 카드 선택 → 슬라이더 조정 → 금지 행동 → 미리보기` 흐름에 맞춰 캐릭터 생성 UI를 persona builder로 재구성했다. 역할/근거/반박/말투는 카드형 radio로 선택하고, 분석성/반박 압력/전문성/문장 길이/입장 수정은 slider로 조정하며, 오른쪽 preview와 빠른 조정 버튼이 같은 `persona` payload를 즉시 반영한다.
- 아티팩트: `src/components/CharacterPanel.tsx`, `src/components/CharacterPanel.test.tsx`, `src/app/globals.css`
- 검증: `npm run lint`, `npm run typecheck`, `npm run test` 25 tests passed, `npm run build`.
- 후속: 실제 LLM Persona Compiler와 샘플 발화 생성은 backend/API 범위가 생기면 연결한다.

- 완료: 캐릭터 생성 UI에서 `personality`/`speechStyle` JSON textarea를 제거하고, 역할/근거/반박/말투/금지 행동 프리셋으로 구조화 `persona` payload를 조립하도록 바꿨다. 토론 생성 결과와 mock mode seed/store도 participant `persona` snapshot을 사용하도록 갱신했다.
- 아티팩트: `src/components/CharacterPanel.tsx`, `src/components/CharacterPanel.test.tsx`, `src/components/DebateComposer.tsx`, `src/components/DebateComposer.test.tsx`, `src/api/generated/backend-schema.d.ts`, `src/server/mock-data`
- 검증: `npm run generate:api`, `npm run lint`, `npm run typecheck`, `npm run test` 23 tests passed, `npm run build`.
- 후속: LLM Persona Compiler, persona 미리보기 발화, 평가 점수 UI는 다음 범위에서 별도 설계한다.

- 완료: `FRONTEND_API_MODE=mock` 개발 모드를 추가해 Next BFF가 Spring API 없이 in-memory 샘플 데이터를 반환하게 했다. 공개 탐색, 인증, workspace, 캐릭터 생성, 토론 생성·시작·턴 생성·완료 흐름은 기존 `/api/*` 경로와 HttpOnly cookie/same-origin 경계를 유지한다.
- 완료: `npm run dev`는 mock mode를 기본으로 주입하고, 실제 Spring API 연동은 `npm run dev:backend`로 분리했다.
- 아티팩트: `scripts/next-dev.mjs`, `src/server/mock-api.ts`, `src/server/mock-data`, `src/server/backend.ts`, `src/server/env.ts`, `src/app/api/mock-mode-routes.test.ts`, `docs/development/platform-mock-data.md`
- 검증: `npm run lint`, `npm run typecheck`, `npm run test` 29 tests passed, `npm run build`, `npm run generate:api` 2회, `docker build -t aichat-frontend:local frontend`.
- 완료: 루트 메인 화면에서 설명형 hero와 긴 소개 문구를 제거하고, 흰색 navigation, 짧은 공지 strip, 대형 검색바, API 기반 카테고리 rail, 16:10 생성형 썸네일 카드 grid 중심으로 재구성했다. `/api/public/*` BFF 흐름과 studio 생성 플로우는 유지했다.
- 아티팩트: `src/components/WorkspaceApp.tsx`, `src/components/WorkspaceApp.test.tsx`, `src/app/globals.css`
- 완료: `/login`도 split hero와 설명 문단을 제거하고, 흰색 top navigation 아래 보라 배경 중앙의 미니멀 인증 form만 받도록 정리했다. 로그인/회원가입 BFF 흐름과 HttpOnly cookie 처리는 변경하지 않았다.
- 아티팩트: `src/components/LoginScreen.tsx`, `src/components/AuthPanel.tsx`, `src/components/AuthPanel.test.tsx`, `src/app/globals.css`
- 완료: mock 카탈로그를 제거하고 `/api/public/*` BFF route 기반 공개 탐색으로 전환했다. 루트 `/`는 공개 토론과 공개 캐릭터를 검색/카테고리 탭으로 탐색하며, 로그인 후 studio에서 캐릭터 생성과 토론 생성·시작·턴 생성·완료를 이어갈 수 있다.
- 완료: Machugi reference raw를 `frontend/references/machugi-io/raw/` ignored folder에 보관하고, 커밋 가능한 분석/토큰 README를 추가했다. UI는 흰색 header/surface와 보라 계열 기하학 배경, 검색바, 카테고리 rail, 카드 그리드로 재구성했다.
- 아티팩트: `src/components/WorkspaceApp.tsx`, `src/components/DebateComposer.tsx`, `src/components/CharacterPanel.tsx`, `src/app/api/public`, `src/app/api/debate-sessions/[sessionId]`, `src/app/login`, `src/app/globals.css`, `references/machugi-io/README.md`, `src/api/generated/backend-schema.d.ts`
- 검증: `npm run generate:api`, `npm run lint`, `npm run typecheck`, `npm run test` 22 tests passed, `npm run build`.
- 검증 제한: `docker build -t aichat-frontend:local frontend`는 Docker Desktop daemon이 떠 있지 않아 실행하지 못했다.
- 후속: public debate/character 상세 페이지를 만들 때 현재 BFF 상세 route를 UI에 연결한다.

## 2026-06-10

- 완료: Next.js 16 App Router, React 19, Tailwind CSS 4 기반 최소 workspace를 추가했다. 회원가입, 로그인, 로그아웃, 내 정보, 캐릭터 목록/생성, 토론 세션 생성, snapshot 결과 표시를 제공했다.
- 완료: JWT를 HttpOnly cookie에 저장하는 BFF, same-origin mutation 검증, refresh single-flight retry, Spring 오류 보존과 upstream 502 정규화를 구현했다.
- 완료: Docker network에서 Spring과 Next를 함께 실행해 회원가입, 캐릭터 생성, 같은 캐릭터 중복 선택, 토론 세션 생성과 snapshot 결과 표시를 브라우저로 확인했다.
- 완료: Machugi형 공개 탐색 구조를 벤치마킹해 비로그인도 볼 수 있는 플랫폼 홈으로 리디자인했다. 추천 토론 카드 그리드, 카테고리 탭, 인기 캐릭터, 최근 세션, 인증 drawer, 로그인 후 제작 스튜디오를 추가했다.
- 완료: 아직 없는 공개 discovery API는 `src/lib/platform-mock.ts`의 명시적 mock 데이터로 분리했고, `frontend/docs/development/platform-mock-data.md`에 향후 API와 mock 제거 조건을 문서화했다.
- 변경: beige-only 색상 제한을 폐기하고 `src/app/globals.css`의 CSS variable 기반 플랫폼 visual system으로 전환했다.
