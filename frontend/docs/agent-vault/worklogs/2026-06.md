# June 2026

## 2026-06-10

- 완료: Next.js 16 App Router, React 19, Tailwind CSS 4 기반 최소 workspace를 추가했다.
  회원가입·로그인·로그아웃, 내 정보, 캐릭터 목록·생성, 토론 세션 생성과 snapshot 결과를
  제공한다.
- 완료: JWT를 HttpOnly cookie에 저장하는 BFF, same-origin mutation 검증, refresh
  single-flight retry, Spring 오류 보존과 upstream 502 정규화를 구현했다.
- 아티팩트: `src/app`, `src/components`, `src/server`, `src/api/generated`,
  `Dockerfile`, `AGENTS.md`
- 검증: OpenAPI type 반복 생성 hash 일치, lint, typecheck, Vitest 21 tests,
  Next production build, standalone Docker build, npm audit 0 vulnerabilities를 통과했다.
- 검증: Docker network에서 Spring과 Next를 함께 실행해 회원가입, 캐릭터 생성, 같은 캐릭터
  중복 선택, 토론 세션 생성과 snapshot 결과 표시를 브라우저로 확인했다.
- 수정: Docker 내부 URL과 외부 `Origin` 차이를 `Host`/forwarded header 기준으로 처리했고,
  빈 목록에서 첫 캐릭터가 추가될 때 참가자 ID가 fallback되도록 회귀를 막았다.
