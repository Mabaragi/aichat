# 2026-06 Worklog

## 2026-06-09 - Spring 7 Jackson converter migration

- 완료: Spring Framework 7에서 제거 예정인 `MappingJackson2HttpMessageConverter`를 `JacksonJsonHttpMessageConverter`로 교체했다. character web DTO와 controller의 JSON tree 타입도 Jackson 3 `tools.jackson.*`로 맞춰 request/response 변환 호환성을 유지했다.
- 아티팩트: `src/main/java/com/example/aichat/character/web/CharacterController.java`, `src/main/java/com/example/aichat/character/web/CharacterResponse.java`, `src/main/java/com/example/aichat/character/web/CreateCharacterRequest.java`, `src/main/java/com/example/aichat/character/web/UpdateCharacterRequest.java`, `src/test/java/com/example/aichat/character/web/CharacterControllerWebMvcTest.java`, `src/test/java/com/example/aichat/user/web/UserControllerWebMvcTest.java`
- 검증: `.\mvnw.cmd test` (74 tests passed)
- 후속: CLI는 Spring MVC converter와 무관하므로 기존 Jackson 2 사용을 유지한다. CLI까지 Jackson 3로 통일할 때는 별도 변경으로 진행한다.

## 2026-06-09 - Generation module boundary refactor

- 완료: 토론 프롬프트 정책을 `debate.domain.DebateTurnPromptBuilder`로 이동하고, 공용 생성 계약을 `generation.application`의 `TextGenerator`, `GenerationRequest`, `GenerationResult`로 재구성했다. mock adapter는 `MockTextGenerator`로 변경했으며 generation 모듈의 debate 의존을 제거했다.
- 아티팩트: `src/main/java/com/example/aichat/debate/domain/DebateTurnPromptBuilder.java`, `src/main/java/com/example/aichat/generation/application/TextGenerator.java`, `src/main/java/com/example/aichat/generation/infrastructure/MockTextGenerator.java`, `docs/agent-vault/decisions/2026-06-09-generation-boundary.md`
- 검증: 관련 테스트 8개와 전체 `.\mvnw.cmd test` 74개가 통과했다. stale reference 검색에서 과거 결정 배경 설명 외 잔여 참조가 없고, generation source/test의 debate import가 0건임을 확인했다.
- 후속: 실제 provider 연동 시 provider-neutral `GenerationRequest`를 유지하고 model ID 매핑은 infrastructure/config에 둔다.

## 2026-06-09 - User package tests and minimal implementation

- 완료: `user` 패키지의 domain/application/web/JPA 계약을 스펙 기준으로 채웠다. 사용자 생성/조회 유스케이스, HTTP 생성 계약, `users` 테이블 매핑과 round-trip 검증 테스트를 추가했다.
- 아티팩트: `src/main/java/com/example/aichat/user/domain/User.java`, `src/main/java/com/example/aichat/user/domain/UserRepository.java`, `src/main/java/com/example/aichat/user/application/CreateUserCommand.java`, `src/main/java/com/example/aichat/user/application/UserView.java`, `src/main/java/com/example/aichat/user/application/CreateUserUseCase.java`, `src/main/java/com/example/aichat/user/application/GetUserUseCase.java`, `src/main/java/com/example/aichat/user/web/CreateUserRequest.java`, `src/main/java/com/example/aichat/user/web/UserController.java`, `src/main/java/com/example/aichat/user/web/UserResponse.java`, `src/main/java/com/example/aichat/user/infrastructure/UserJpaEntity.java`, `src/main/java/com/example/aichat/user/infrastructure/UserJpaRepository.java`, `src/main/java/com/example/aichat/user/infrastructure/JpaUserRepositoryAdapter.java`, `src/test/java/com/example/aichat/user/domain/UserTest.java`, `src/test/java/com/example/aichat/user/application/UserUseCaseTest.java`, `src/test/java/com/example/aichat/user/web/UserControllerWebMvcTest.java`, `src/test/java/com/example/aichat/user/infrastructure/UserJpaRepositoryTest.java`
- 검증: `.\mvnw.cmd test`
- 후속: `GET /api/users/{userId}` 같은 user 조회 HTTP 계약은 스펙 문서에 아직 명시가 없어 이번 변경에서는 유스케이스까지만 고정했다.

## 2026-06-09 - Generation prompt and mock client tests

- 완료: `generation` 패키지에 `PromptBuilder`, `LlmGenerateCommand`, `LlmGenerateResult`, `MockLlmClient`를 위한 테스트를 추가하고, 스펙의 프롬프트 템플릿과 mock 응답 규칙을 고정했다. 테스트가 통과하도록 최소 구현도 함께 채웠다.
- 아티팩트: `src/main/java/com/example/aichat/generation/application/PromptBuilder.java`, `src/main/java/com/example/aichat/generation/domain/LlmGenerateCommand.java`, `src/main/java/com/example/aichat/generation/domain/LlmGenerateResult.java`, `src/main/java/com/example/aichat/generation/infrastructure/MockLlmClient.java`, `src/test/java/com/example/aichat/generation/application/PromptBuilderTest.java`, `src/test/java/com/example/aichat/generation/domain/LlmGenerateCommandTest.java`, `src/test/java/com/example/aichat/generation/domain/LlmGenerateResultTest.java`, `src/test/java/com/example/aichat/generation/infrastructure/MockLlmClientTest.java`
- 검증: `.\mvnw.cmd test`
- 후속: 실제 LLM 연동이 들어오면 `LlmGenerateCommand` 필드와 `MockLlmClient` 응답 정책을 그 계약에 맞춰 확장한다.

## 2026-06-09 - Remove EIP and DynamoDB from AWS deploy flow

- 완료: `infra/bootstrap`에서 DynamoDB state lock table과 관련 IAM 권한/출력을 제거하고, `infra/app`에서 EIP 리소스와 association을 삭제한 뒤 인스턴스의 ephemeral public IP를 그대로 출력하도록 바꿨다. GitHub Actions release workflow는 S3 backend `use_lockfile = true`만 쓰도록 정리했고, 운영 가이드도 EIP/DynamoDB 없이 맞췄다.
- 아티팩트: `infra/bootstrap/main.tf`, `infra/bootstrap/outputs.tf`, `infra/bootstrap/variables.tf`, `infra/app/main.tf`, `infra/app/outputs.tf`, `.github/workflows/release.yml`, `docs/development/aws-cicd.md`
- 검증: `terraform fmt -check -recursive infra`, `terraform -chdir=infra/bootstrap init -backend=false -input=false && terraform -chdir=infra/bootstrap validate`, `terraform -chdir=infra/app init -backend=false -input=false && terraform -chdir=infra/app validate`
- 후속: 없음

## 2026-06-09 - Debate domain test coverage

- 완료: `debate` 도메인에 `DebateSession`, `DebateParticipant`, `DebateTurn` 검증 로직을 추가하고, 세션 생성/시작/완료 규칙과 턴 순서 계산을 고정하는 테스트를 작성했다. `GenerateNextTurnUseCase`에는 첫 참가자 선택, 라운드 계산, 완료 조건 계산을 위한 순수 계산 메서드를 넣었다.
- 아티팩트: `src/main/java/com/example/aichat/debate/domain/DebateSession.java`, `src/main/java/com/example/aichat/debate/domain/DebateTurn.java`, `src/main/java/com/example/aichat/debate/application/GenerateNextTurnUseCase.java`, `src/test/java/com/example/aichat/debate/domain/DebateParticipantTest.java`, `src/test/java/com/example/aichat/debate/domain/DebateSessionTest.java`, `src/test/java/com/example/aichat/debate/domain/DebateTurnTest.java`, `src/test/java/com/example/aichat/debate/application/GenerateNextTurnUseCaseTest.java`
- 검증: `.\mvnw.cmd test`
- 후속: `CreateDebateSessionUseCase`와 `StartDebateSessionUseCase`에 이 도메인 규칙을 연결하면 세션 생성 API 쪽도 같은 규칙을 재사용할 수 있다.

## 2026-06-09 - Character controller wiring tests

- 완료: `character` web controller에 CRUD endpoint를 연결하고, controller → use case 요청/응답 매핑을 검증하는 MockMvc 기반 테스트를 추가했다. `CharacterResponse`는 application view를 web JSON으로 다시 변환하도록 맞췄다.
- 아티팩트: `src/main/java/com/example/aichat/character/web/CharacterController.java`, `src/main/java/com/example/aichat/character/web/CharacterResponse.java`, `src/test/java/com/example/aichat/character/web/CharacterControllerWebMvcTest.java`
- 검증: `.\mvnw.cmd test`
- 후속: 없음

## 2026-06-09 - Backend TDD guideline

- 완료: 백엔드 작업에서 TDD를 우선 적용하는 개발 가이드를 추가하고, `AGENTS.md`, `docs/development/README.md`, `docs/agent-vault/INDEX.md`에서 연결했다.
- 아티팩트: `docs/development/backend-tdd-guideline.md`, `AGENTS.md`, `docs/development/README.md`, `docs/agent-vault/INDEX.md`
- 검증: 문서만 변경했으므로 코드 테스트는 생략한다. 문서 링크와 git 상태를 확인한다.
- 후속: 도메인별 TDD 흐름이 더 구체화되면 해당 도메인 개발 가이드로 분리한다.

## 2026-06-09 - aichat AWS deploy execution

- 완료: `infra/bootstrap`와 `infra/app`을 `ap-northeast-2`에 실제 적용하고, ECR 이미지 `322eb4cff8a9cb38005c1599f1945f9c53320cc8`를 push한 뒤 EC2 `i-07496c122ece84446`에 SSM으로 컨테이너를 재배포했다. 공인 IP `54.116.186.206:8080`에서 `404` 응답을 확인해 리스너와 네트워크 경로가 살아 있음을 검증했다.
- 아티팩트: `arn:aws:iam::049882582319:role/aichat-gha-terraform`, `arn:aws:iam::049882582319:role/aichat-gha-deploy`, `049882582319.dkr.ecr.ap-northeast-2.amazonaws.com/aichat:322eb4cff8a9cb38005c1599f1945f9c53320cc8`, `i-07496c122ece84446`, `54.116.186.206`
- 검증: `terraform -chdir=infra/bootstrap apply -auto-approve`, `terraform -chdir=infra/app apply -auto-approve`, `docker build -t 049882582319.dkr.ecr.ap-northeast-2.amazonaws.com/aichat:322eb4cff8a9cb38005c1599f1945f9c53320cc8 .`, SSM remote `docker ps` 및 `curl` 확인
- 후속: GitHub Actions release 자동 트리거는 별도 점검 여지가 있지만, 현재 배포는 수동 AWS 경로로 성공 완료했다.

## 2026-06-09 - GitHub Actions Terraform CI/CD

- 완료: `aichat`를 `EC2 + SSM + ECR` 기준으로 배포하도록 Docker packaging, Terraform bootstrap/app 분리, GitHub Actions CI/release, 운영 가이드를 추가했다. `main` 기준 SHA 태그만 배포하고, SSM stdout/stderr와 외부 HTTP 상태를 확인하는 흐름으로 자동화를 묶었다.
- 아티팩트: `Dockerfile`, `.dockerignore`, `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `infra/bootstrap/versions.tf`, `infra/bootstrap/main.tf`, `infra/bootstrap/outputs.tf`, `infra/app/backend.tf`, `infra/app/main.tf`, `infra/app/user_data.sh.tftpl`, `docs/development/aws-cicd.md`, `docs/development/README.md`
- 검증: `.\mvnw.cmd test`, `terraform fmt -check -recursive infra`, `terraform init -backend=false -input=false` 및 `terraform validate` for `infra/bootstrap` and `infra/app`, `docker build -t aichat:local .`
- 후속: bootstrap은 최초 1회 AWS 관리자 권한으로 적용한 뒤, GitHub repository variables에 Terraform/Deploy role ARN을 넣고 release workflow를 사용한다.

## 2026-06-09 - Character JPA adapter implementation

- 완료: `CharacterRepository` 포트를 구현하는 `JpaCharacterRepositoryAdapter`를 추가하고, `InMemoryCharacterRepository`를 `dev` 프로파일 전용으로 제한했다. JPA entity 접근자를 보강해 domain/entity round-trip과 저장소 계약 테스트가 adapter 기준으로 동작하도록 연결했다.
- 아티팩트: `src/main/java/com/example/aichat/character/infrastructure/JpaCharacterRepositoryAdapter.java`, `src/main/java/com/example/aichat/character/infrastructure/CharacterJpaEntity.java`, `src/main/java/com/example/aichat/character/infrastructure/InMemoryCharacterRepository.java`, `src/test/java/com/example/aichat/character/infrastructure/CharacterJpaRepositoryTest.java`
- 검증: `.\mvnw.cmd test`
- 후속: 없음

## 2026-06-08 - Character value object conversion

- 완료: `character` 도메인의 `personality`와 `speechStyle`를 raw `String` 대신 값 객체로 전환했다. CLI 입력은 JSON 검증 후 값 객체로 변환하고, `CharacterView`는 기존 문자열 출력 계약을 유지하도록 평탄화했다.
- 아티팩트: `src/main/java/com/example/aichat/character/domain/Personality.java`, `src/main/java/com/example/aichat/character/domain/SpeechStyle.java`, `src/main/java/com/example/aichat/character/domain/Character.java`, `src/main/java/com/example/aichat/character/application/CharacterView.java`, `src/main/java/com/example/aichat/cli/character/CharacterCommandSupport.java`, `src/test/java/com/example/aichat/character/domain/CharacterTest.java`, `src/test/java/com/example/aichat/character/application/CharacterUseCaseTest.java`, `src/test/java/com/example/aichat/character/infrastructure/CharacterRepositoryContractTest.java`, `src/test/java/com/example/aichat/character/infrastructure/InMemoryCharacterRepositoryTest.java`, `src/test/java/com/example/aichat/character/domain/PersonalityTest.java`, `src/test/java/com/example/aichat/character/domain/SpeechStyleTest.java`, `src/test/java/com/example/aichat/cli/CliCommandTest.java`
- 검증: `.\mvnw.cmd test`
- 후속: 없음

## 2026-06-08 - Character repository contract tests

- 완료: `character` 저장소의 JPA 어댑터가 만족해야 할 계약 테스트를 추가했다. 새로 저장한 캐릭터의 round-trip, owner 기준 목록 필터링, 기존 캐릭터 update, delete 동작을 검증하는 abstract contract test를 작성했다.
- 아티팩트: `src/test/java/com/example/aichat/character/infrastructure/CharacterRepositoryContractTest.java`
- 검증: `.\mvnw.cmd test -q`
- 후속: 실제 JPA adapter가 생기면 이 contract test를 상속해 persistence mapping을 연결한다.

## 2026-06-08 - DebateParticipant model selection

- 완료: `DebateParticipant` 도메인에 미리 정해진 모델 선택값을 추가했다. `ParticipantModel` enum을 도입하고 참가자 모델을 `MOCK`, `FAST`, `BALANCED`, `QUALITY` 중 하나로 고를 수 있게 했다.
- 아티팩트: `src/main/java/com/example/aichat/debate/domain/DebateParticipant.java`, `src/main/java/com/example/aichat/debate/domain/ParticipantModel.java`
- 검증: `.\mvnw.cmd test -q`
- 후속: 참가자 생성 흐름과 세션 생성 DTO에 모델 선택값을 연결한다.

## 2026-06-08 - Character domain alignment

- 완료: `character` 도메인을 문서 기준으로 정리했다. `Character`에 `personality`와 `visibility` 정규화, owner/name/description/timestamp 검증, update 메서드를 추가하고, repository 계약과 web DTO를 캐릭터 스펙에 맞게 채웠다.
- 아티팩트: `src/main/java/com/example/aichat/character/domain/Character.java`, `src/main/java/com/example/aichat/character/domain/CharacterRepository.java`, `src/main/java/com/example/aichat/character/web/CreateCharacterRequest.java`, `src/main/java/com/example/aichat/character/web/UpdateCharacterRequest.java`, `src/main/java/com/example/aichat/character/web/CharacterResponse.java`, `src/test/java/com/example/aichat/character/domain/CharacterTest.java`
- 검증: `.\mvnw.cmd test -q`
- 후속: `CharacterController`와 application/infrastructure 구현을 붙여 실제 API 흐름을 연결한다.

## 2026-06-08 - Agent documentation structure

- 완료: 루트 `AGENTS.md`, agent vault, workflow, decision, worklog, human learning 문서 구조를 추가했다.
- 아티팩트: `AGENTS.md`, `docs/README.md`, `docs/agent-vault/INDEX.md`, `docs/learnings/README.md`
- 검증: 문서만 변경했으므로 코드 테스트는 생략한다. 문서 링크와 git 상태를 확인한다.
- 후속: 새 도메인 구현이나 반복 피드백이 생기면 관련 guide 또는 decision note로 승격한다.

## 2026-06-08 - Spec source of truth and participant model sync

- 완료: `AGENTS.md`에 `ai_debate_platform_mvp_spec.md`를 진실의 근원으로 보는 규칙을 추가했고, `DebateParticipant` 스펙을 현재 `ParticipantModel` 중심 모델에 맞춰 갱신했다.
- 아티팩트: `AGENTS.md`, `ai_debate_platform_mvp_spec.md`
- 검증: 문서만 변경했으므로 코드 테스트는 생략한다. 참가자 모델 충돌 키워드와 git 상태를 확인한다.
- 후속: 캐릭터 기반 참가자를 다시 도입하면 같은 변경에서 스펙과 구현을 함께 수정한다.

## 2026-06-08 - Character CLI foundation

- 완료: `picocli` 기반 `character` CLI 진입점과 use case 연결을 추가했다.
- 아티팩트: `pom.xml`, `src/main/java/com/example/aichat/cli/*`, `src/main/java/com/example/aichat/character/application/*`, `src/main/java/com/example/aichat/character/infrastructure/InMemoryCharacterRepository.java`, `docs/agent-vault/guides/character-cli.md`, `HELP.md`
- 검증: 구현 후 `.\mvnw.cmd test`를 실행할 예정이다. CLI help, JSON/text 출력, not-found exit path를 테스트로 확인한다.
- 후속: REST controller가 채워질 때 동일한 application contract를 재사용하도록 web adapter를 연결한다.

## 2026-06-08 - Character persistence test coverage

- 완료: `CharacterJpaRepository`와 `CharacterJpaEntity`용 SQLite 통합 테스트를 추가했고, 기존 `InMemoryCharacterRepositoryTest`를 포함한 저장소 계약 테스트를 유지했다. JPA 스키마 컬럼, save/find/update/delete, owner 필터링을 검증한다.
- 아티팩트: `src/test/java/com/example/aichat/character/infrastructure/CharacterJpaRepositoryTest.java`, `src/test/resources/application-test.yaml`, `pom.xml`
- 검증: `.\mvnw.cmd test`
- 후속: 실제 JPA adapter가 도메인 `CharacterRepository` 포트에 연결되면 이 테스트를 그대로 재사용한다.
