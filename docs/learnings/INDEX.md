# Learnings Index

- `topics/java-records-value-objects.md`: Java `record`를 DTO, command, view, value object에 쓰는 이유와 `of` factory의 의미를 정리한다.
- `topics/spring-security-authenticated-user.md`: Spring Security filter chain, `@RequestBody`, `Authentication`의 처리 경로와 JWT `sub`를 `RequestActor`로 전달하는 이유를 설명한다.

```md
- `notes/YYYY-MM-DD-short-title.md`: <무엇을 배웠는지 한 줄 요약>.
- `topics/topic-name.md`: <반복 참고할 주제 요약>.
```
